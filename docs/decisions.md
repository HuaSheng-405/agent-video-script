# 设计决策记录（docs/decisions.md）

> 每条决策记录「选择 + 理由 + 代价」，是面试深挖的标准答案来源。
> 第 3 周主项目 v1.0 开工时，这里会长出更多条目。

## 2026-08 v0.2 验证性原型的 6 个决策

| # | 决策点 | 选择 | 理由 | 代价/风险 |
|---|---|---|---|---|
| 1 | 编排方式 | 父图固定流水线 + 编剧子图 | 视频脚本生产强顺序依赖，显式边可控可测评 | 灵活性低于 Supervisor，但本场景不需要 |
| 2 | 质检架构 | 规则层(代码) + LLM 打分层(1-5) | day06 实测 LLM 硬判 PASS/FAIL 会标准漂移（追着自己建议的钩子跑）；打分+中位数抗噪（RAGAS 方法论同源） | 规则层需校准，主观项仍有噪声 |
| 3 | 规则阈值 | 口播词 150~450 字（60 秒 ≈ 250 字） | 原 100~400 为拍脑袋，实测脚本含格式标记长 500+ 字连续误杀——阈值必须来自业务常识（语速 220-260 字/分）+ 实测分布 | 仍需按真实脚本微调 |
| 4 | 质检批注 | 必须可执行：「删减铺垫、保留钩子」而非「字数超标」 | 批注质量决定回退质量（day06 教训）；不可执行批注导致越改越差（实测：1087→429→558 开场被裁） | 批注模板需持续打磨 |
| 5 | 素材检索 | MCP 跨进程调用（llm env 检索 / python3.11 Agent） | 环境隔离、复用 rag-video-script 检索层、未来可多 Agent 共用（N+M） | 多一个进程就要管它的健康状态；工具为异步，整链必须 ainvoke |
| 6 | 回退策略 | ≤2 次后强制放行 | 防死循环烧 token（day06 retry_count 同构） | 极端情况输出非最优稿 |

## 2026-08-26 v1.1 补充决策

| # | 决策点 | 选择 | 理由 |
|---|---|---|---|
| 7 | 素材 Agent 检索范围 | **文字脚本参考片段/事实依据**（现有 RAG 库），非画面视觉素材 | v1.0 曾把素材需求定义为「画面资源」，被数据现实纠正：素材库只有文字脚本。设计必须贴合数据现实 |
| 8 | 视觉素材库（图片/视频） | 列入 v1.2 展望（接入 Pexels/剪映素材库 API） | 模块化红利：换检索源只改 agents_material.py 一个模块 |

## v1.2 设计草案：多轮上下文管理（三层记忆体系）

**问题**：多轮改稿会话下，context window 有限（DeepSeek 64K~128K），全量对话历史会撞墙。

**设计**（三层，两层已有地基）：
1. **状态层（已有）**：plan/script/spec 是结构化产物，存 State 不占对话空间——「已确认的产物」不进上下文，只有「增量讨论」进
2. **窗口层（v1.2 接入）**：`SummarizationMiddleware` 自动压缩超限消息为摘要——兜底，而非主解
3. **持久层（已有）**：checkpoint（SQLite）保证跨会话不丢

**多轮协议草案（v2，修正 v1 漏洞）**：
- v1 缺陷：上下文只留「上一轮产物 + 最近反馈」，用户**早期约束**会在多轮迭代中被洗掉（实测推演发现）
- v2 修正：记忆分三类治理——**约束（Requirements 清单，累积、只增不减，除非用户明确撤销）/ 产物（plan/script/spec，可替换）/ 对话增量（最近 2 轮反馈，可压缩）**
- 上下文协议：当前产物 + **完整需求清单** + 最近 2 轮反馈摘要；更老历史 → checkpoint 可查；对话超限 → middleware 压缩
- 配套护栏：QC 增加「需求符合度」检查（LLM judge 逐条核对 Requirements，违反早期约束照样打回）——**约束的护栏是机制，不是记忆**
- **约束冲突处理（v3）**：最新意图优先，但分级——显式冲突（语义相反）走 HITL 反问确认（interrupt，day06b 机制复用）；隐含冲突（同维度微调）后者覆盖前者；无法判断则执行最新并标注。数据层：约束只标记 `superseded` 不物理删除（可追溯、可回滚）
- **回滚协议（v4）**：三层回滚——产物层（State 版本历史队列：script/spec/requirements 快照一起存）、状态层（checkpoint 原生回放，get_state_history 恢复到指定检查点）、语义层（版本树：回滚是分叉不删除，based_on 标注）。关键联动：回滚产物必须同步回滚约束快照（三类记忆一起动）；输出带版本标识（v3 based_on v1），用户才能指认回滚目标
**核心原则**：压缩是兜底，分类治理才是主解——约束累积、产物可替换、对话可压缩；**冲突确认优先于自动覆盖**。

## 2026-08-28 v1.2.1 补充决策：视觉素材（Pexels）落地

| # | 决策点 | 选择 | 理由 |
|---|---|---|---|
| 9 | 视觉检索源 | Pexels 视频 API（`/videos/search`） | 剪映 API 不对个人开放；Pexels 免费、无需审核、支持按query检索视频与时长/分辨率 |
| 10 | 检索关键词语言 | **英文** | Pexels 资源以英文标签为主，中文 query 命中率极低（实测教训：中文搜不到） |
| 11 | 视觉检索是否为独立节点 | **独立 media 节点**（`pipeline/agents_media.py` + `media_server.py`） | 文字检索（RAG 库）与视觉检索（Pexels）数据源根本不同，耦合在一个节点违背「换源只改一个模块」；拆开后两者可分别降级 |
| 12 | 视觉候选排序 | 按镜头时长 **±50% 过滤**，再按「|候选时长-镜头时长|」升序 | 镜头长度是硬约束（每条 5~15s），先硬过滤再贴近排序，避免塞进一条 60s 长视频 |
| 13 | 视觉检索调用形态 | `search_videos_batch(queries)` 一次会话批量检索 | 避免 N 镜头 = N 次跨进程 MCP 会话（v1.1 踩过：批检索把耗时 252s→... 的核心优化；单条子进程开销极大） |
| 14 | 视频候选的消费方 | 本轮**不做 Web UI**，仅产出 `video_candidates`（供后续选择/本地合成） | 先把「生成候选」链路跑通，UI 属交互层，可后置；本地合成（FFmpeg+TTS）为 v1.2.2 终态 |

**media 节点降级规则**：`media_tools=None` 时整节点 no-op（返回空 `video_candidates`），保证无 Pexels key / 无网络时文本模式照常跑，评估不受影响。


1. **MCP server 日志必须走 stderr**：print 进 stdout 会污染 JSON-RPC 协议流（实测 UnicodeDecodeError）
2. **含 MCP/网络工具的 Agent 整链必须 async**：StructuredTool 不支持同步调用（NotImplementedError）
3. **跨环境 MCP 版本必须一致**：client/server 协议握手会失败
4. **子图节点函数注解要用子图自己的 State**：外层 State 含 Annotated 通道时返回值会被 schema 丢弃
5. **素材需求必须与数据源一致**：检索「库里没有的东西」等于空转——设计先问「我有什么」，再定义需求（v1.1 实测教训）
